name: QRPH Live Checker

on: [push, pull_request]

jobs:
  Live_Transaction_Checker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16' # or your preferred version

      - name: Install dependencies
        run: npm ci

      - name: Run Cypress tests
        run: npx cypress run --spec "cypress/e2e/TransactionChecker/QRPH_Deposit_Transaction.cy.js"

      - name: Run Cypress tests
        run: npx cypress run --spec "cypress/e2e/TransactionChecker/Instapay_Withdrawal_Transaction.cy.js"

      - name: Run Cypress tests
        run: npx cypress run --spec "cypress/e2e/TransactionChecker/JPay_Deposit_Transaction.cy.js"

      - name: Upload file as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Live_Transaction_Checker # Name of the artifact/file
          path: cypress/e2e/Reports/LiveTransactionChecker/LiveTransactionChecker.xlsx

      - name: Upload Cypress screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-transaction-screenshots
          path: cypress/screenshots

  Topup_Checker:
    runs-on: ubuntu-latest
        
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16' # or your preferred version
        
      - name: Install dependencies
        run: npm ci
        
      - name: Run Cypress tests
        run: npx cypress run --spec "cypress/e2e/BalanceChecker/Topup_Balance.cy.js"
        
      - name: Upload file as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Topup_Balance_Checker # Name of the artifact/file
          path: |
            cypress/e2e/Reports/BalanceChecker/Topup_Balance.xlsx

      - name: Upload Cypress screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-balance-screenshots
          path: cypress/screenshots

  Settlement_Checker:
    runs-on: ubuntu-latest
        
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16' # or your preferred version
        
      - name: Install dependencies
        run: npm ci

      - name: Run Cypress tests
        run: npx cypress run --spec "cypress/e2e/BalanceChecker/Settlement_Balance.cy.js"
        
      - name: Upload file as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Settlement_Balance_Checker # Name of the artifact/file
          path: |
            cypress/e2e/Reports/BalanceChecker/Settlement_Balance.xlsx

      - name: Upload Cypress screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-balance-screenshots
          path: cypress/screenshots



